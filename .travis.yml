language: generic 
sudo: required
services: 
  - docker 

before_install: 
  - docker build -t fentonreid/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true fentonreid/docker-react npm run test